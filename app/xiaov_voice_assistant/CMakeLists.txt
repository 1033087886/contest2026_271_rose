if(CONFIG_EXAMPLES_XIAOV)
  set(XIAOV_DEPENDENCIES)
  if(CONFIG_EXAMPLES_XIAOV_LVGL)
    list(APPEND XIAOV_DEPENDENCIES lvgl)
    set(XIAOV_CJK_FONT_SOURCE assets/xiaov_cjk_font_16.c)
  endif()
  if(CONFIG_EXAMPLES_XIAOV_KWS)
    list(APPEND XIAOV_DEPENDENCIES tflite_micro)
    add_compile_definitions(
      TFLITE_EMULATE_FLOAT
      TF_LITE_DISABLE_X86_NEON
      TF_LITE_STRIP_ERROR_STRINGS)
    set(XIAOV_KWS_INCLUDE
      ${NUTTX_APPS_DIR}/mlearning/tflite-micro/tflite-micro
      ${NUTTX_APPS_DIR}/math/gemmlowp/gemmlowp
      ${NUTTX_APPS_DIR}/math/kissfft/kissfft
      ${NUTTX_APPS_DIR}/math/ruy/ruy
      ${NUTTX_APPS_DIR}/system/flatbuffers/flatbuffers/include)
    set(XIAOV_KWS_SOURCES
      xiaov_capture_gain.c
      xiaov_kws_frontend.c
      xiaov_tflm_kws_adapter.cc
      xiaov_kws_model.cc
      core/src/kws_controller.c)
  endif()
  if(CONFIG_EXAMPLES_XIAOV_OFFLINE_COMMANDS)
    set(XIAOV_OFFLINE_COMMAND_SOURCES
      core/src/offline_command.c
      xiaov_command_model.cc)
  endif()
  if(CONFIG_EXAMPLES_XIAOV_AUDIO_ALSA)
    set(XIAOV_AUDIO_SOURCES
      xiaov_alsa_adapter.c
      xiaov_alsa_backend.c)
    set(XIAOV_ALSA_INCLUDE
      ${NUTTX_APPS_DIR}/vendor/allwinnertech/chips/r528/drivers/rtos-hal/include/hal
      ${NUTTX_APPS_DIR}/vendor/allwinnertech/chips/r528/drivers/rtos-hal/include/osal
      ${NUTTX_APPS_DIR}/vendor/allwinnertech/chips/r528/drivers/rtos-hal/hal/source)
  else()
    set(XIAOV_AUDIO_SOURCES xiaov_media_adapter.c)
  endif()
  nuttx_add_application(
    NAME
    ${CONFIG_EXAMPLES_XIAOV_PROGNAME}
    DEPENDS
    ${XIAOV_DEPENDENCIES}
    SRCS
    xiaov_main.c
    xiaov_protocol.c
    xiaov_skills.c
    ${XIAOV_AUDIO_SOURCES}
    xiaov_local_wav.c
    xiaov_music_adapter.c
    xiaov_network_adapter.c
    xiaov_power_adapter.c
    xiaov_service.c
    xiaov_ui.c
    ${XIAOV_CJK_FONT_SOURCE}
    ${XIAOV_KWS_SOURCES}
    ${XIAOV_OFFLINE_COMMAND_SOURCES}
    core/src/audio_frame.c
    core/src/audio_rate_converter.c
    core/src/audio_visualizer.c
    core/src/conversation.c
    core/src/event_queue.c
    core/src/music_player.c
    core/src/reminder_queue.c
    core/src/ring_buffer.c
    core/src/runtime.c
    core/src/state_machine.c
    core/src/stream_markdown.c
    core/src/vad.c
    INCLUDE_DIRECTORIES
    .
    core/include
    ${XIAOV_ALSA_INCLUDE}
    ${XIAOV_KWS_INCLUDE}
    ${CMAKE_SOURCE_DIR}/../frameworks/multimedia/media/include
    ${NUTTX_APPS_DIR}/netutils/libwebsockets/libwebsockets/include
    ${NUTTX_APPS_DIR}/netutils/libwebsockets
    STACKSIZE
    ${CONFIG_EXAMPLES_XIAOV_STACKSIZE}
    PRIORITY
    ${CONFIG_EXAMPLES_XIAOV_PRIORITY})
endif()
